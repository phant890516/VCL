# AGENTS.md

このリポジトリで Agent が作業するときのルール。

## 基本方針

- 一時ファイルを作成した場合は、作業完了前に必ず削除する。
- 実装内容を変更した場合は、`docs` 配下の要件定義書を必ず更新する。
- 既存の変更を勝手に戻さない。自分が触っていない差分は、ユーザーの作業として扱う。

## コメント方針

- 新しく書くコードコメントは、原則として日本語で書く。
- コメントは「なぜこの処理が必要か」「後から読む人が迷いやすい点」を説明するために使う。
- 処理をそのまま読み上げるだけのコメントは書かない。
- 既存コードに文字化けコメントがある場合、触る範囲では読みやすい日本語へ直す。

## Git 方針

- Git にコミットするときのコミットメッセージは、原則として日本語で書く。
- push や変更内容の説明を行う場合も、内容が伝わる日本語で簡潔に書く。

## フロントエンド実装方針

- UI コンポーネント、ページ、画面デザイン、CSS、アニメーションなどフロントエンドの見た目や操作感を変更する場合は、必ず `frontend-design` スキルを使う。
- JS ファイルに画面 HTML を直書きしない。
- 画面の静的 HTML は `template.html` や `*.template.html` に分離し、JS では読み込んだテンプレートを使う。
- 動的に増えるカード、リスト、セル、トーストなどは HTML 側の `<template>` を複製し、JS では `textContent`、`classList`、`dataset`、`style` など必要な値だけ差し込む。
- `innerHTML = \`...\`` のようなテンプレートリテラルで HTML を組み立てない。
- `insertAdjacentHTML` で HTML 文字列を差し込まない。
- HTML 表示が必要な説明文や解説文は、JS 内の文字列ではなく `.html` ファイルに分離して読み込む。
- 例外的に `container.innerHTML = template` のように、別ファイルから読み込んだテンプレート全体を差し込む用途は許可する。

## 画像生成方針

- 画像生成が必要な場合は、ChatGPT の画像生成機能を使って画像ファイルを作成する。
- 画像を作る目的で、SVG をコードで組み立てたり、Canvas、画像変換ライブラリ、SVG から PNG への変換ライブラリなどに代替させない。
- 画像生成ライブラリの依存不足を理由に、別ルートで SVG を生成して PNG 化する対応は行わない。
- 既存画像の圧縮、リサイズ、ファイル形式変換など、生成済み画像の軽微な加工が必要な場合だけ、事前に目的を明確にしてからツールやライブラリを使う。

## ドキュメント更新

- 機能追加、DB スキーマ変更、API 追加、画面仕様変更を行った場合は、`docs/requirements.md` に追記する。
- 要件定義書を更新するときは、文書バージョンと改訂履歴も合わせて更新する。
- DB のテーブル、保存先、API 対応、RLS などを変更した場合は、`docs/database_design.md` も更新する。
- 画面の表示内容、操作、遷移、保存処理を変更した場合は、`docs/screen_spec.md` も更新する。
