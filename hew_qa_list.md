# HEW (Hardening II Security Engineering Workshop) / 技術展示会用 QAリスト

本ドキュメントは、展示会や審査会における想定質問とその回答集です。
プロジェクトの特性（教育×IoT×3D）および現在の実装仕様（2026年3月時点）に基づいています。

---

## 1. プロダクトコンセプト・教育的価値

### Q: なぜバーチャルで化学実験をする必要があるのですか？リアルで良くないですか？
> **A:** リアルの実験は準備・片付けの手間がかかる上、危険な薬品（濃硫酸など）を扱うリスクやコストの問題があります。
> VCLは**「失敗しても安全」「高価な試薬も使い放題」**というデジタルの利点を活かしつつ、物理デバイスによる**「実験の手触り感（注ぐ、振る）」**を残すことで、座学と実実験の間を埋める新しい学習体験を提供します。

### Q: 既存の動画教材やアプリと何が違いますか？
> **A:** **「身体性」の有無**です。
> 単に画面をクリックするのではなく、試験管型コントローラーを「傾けて注ぐ」「振って混ぜる」という身体動作を伴うことで、記憶への定着率（没入感）を高めることを狙っています。また、3D空間での実験結果（色変化、沈殿、気体発生）をリアルタイムにフィードバックします。

---

## 2. ソフトウェア・アーキテクチャ

### Q: システム全体の構成（アーキテクチャ）を教えてください。
> **A:** Node.jsを中心とした構成です。
> - **Frontend:** Vite + Vanilla JS + Three.js (SPA)
> - **Backend:** Node.js (Express) + Socket.IO
> - **Database:** SQLite (プロトタイプとしての可搬性を重視)
> - **Device:** ESP32 (Wi-Fi) および Nintendo Joy-Con (Bluetooth)
>
> バックエンドは保守性を高めるため、**三層アーキテクチャ（Controller / Service / Repository）**を厳格に適用しています。

### Q: なぜこの規模で三層アーキテクチャを採用したのですか？
> **A:** **ロジックの分離と拡張性のため**です。
> センサーデータの加工ロジック（Service層）と、APIの入出力（Controller層）、データの永続化（Repository層）を分けることで、例えば「センサー処理のアルゴリズムを変えたい」といった変更が、他の層に影響を与えずに安全に行えます。また、テスト（Unit Test）も書きやすくなります。

### Q: デバイスとブラウザの通信ラグ（遅延）対策は？
> **A:** **Socket.IOによるリアルタイム双方向通信**を採用し、低遅延化を図っています。
> さらに、3D描画側（Three.js）では、受信した角度データをそのまま適用するのではなく、線形補間（Lerp）を用いることで、通信パケット間のガタつきを滑らかに補正しています。

---

## 3. ハードウェア・デバイス制御

### Q: 物理デバイス（試験管）の仕組みはどうなっていますか？
> **A:** 自作デバイスと既製品の2パターンをサポートしています。
> 1. **Test Cube (自作):** ESP32マイコンと加速度センサー(MPU6050)を内蔵し、Wi-Fi経由でサーバーにデータを送信します。
> 2. **Joy-Con:** Nintendo Joy-ConをBluetoothでPCに接続し、Node.js (`node-hid`) でジャイロデータを読み取っています。
>
> これらはサーバー上で統合され、Socket.IOを通じてフロントエンドに一元的に配信されます。

### Q: センサーのドリフト（時間経過でのズレ）はどう解決しましたか？
> **A:** **「加速度センサーのみを使用する」ロジックに変更**しました。
> 当初はジャイロセンサーを使用していましたが、積分誤差によるドリフトが発生しました。実験動作（注ぐ、振る）においては重力方向に対する傾きがわかれば十分であるため、加速度ベクトルから角度を算出する方式に切り替え、ドリフトを根本的に解消しました。
> (参照: `test_tube_talk.md` 2026-02-13 議事録)

### Q: 試験管デバイスを持つ向きに制約はありますか？
> **A:** はい、あります。
> 円筒形であるため、ロール回転の軸を特定するために**「識別LED（またはUSBポート）を自分の方に向けて持つ」**という運用ルールを設けています。これは現在の安価なセンサー構成における物理的な仕様限界です。

### Q: 通信プロトコルが HTTP と WebSocket で混在しているのはなぜ？
> **A:** **デバイスごとの最適化の結果**です。
> - **ESP32:** 実装の容易さと安定性を重視し、HTTP POST (`/gyro`) を採用しました。
> - **Joy-Con:** 連続的なデータストリームが必要なため、HID接続を採用。
> - **Frontendへの配信:** ユーザー体験に関わるため、Socket.IOによるWebSocket通信で統一しています。
>
> つまり、入り口はバラバラでも、サーバー内部でデータを正規化し、出口（フロントエンド）へは統一された形式で流す設計にしています。

---

## 4. セキュリティ・安全性

### Q: 子どもが使うことを想定した場合の配慮は？
> **A:**
> - **入力検証:** バックエンドのService層で不正なセンサー値（NaNや異常値）をフィルタリングしています。
> - **物理的安全性:** 試験管デバイスは低電圧（3.7Vバッテリー）で動作し、筐体はPLA樹脂製で割れる心配がありません。
> - **ネットワーク:** 学校環境などを想定し、外部インターネットへの接続を必要とせず、ローカルネットワーク内で完結して動作可能です（外部API依存なし）。

---

## 5. 今後の展望

### Q: 新しい実験や薬品を追加するのは簡単ですか？
> **A:** はい。
> 薬品データは `periodic.json` や `chemicalMapping.js` で外出しされており、JSONデータを追加するだけで基本的な物理定数（色、分子量など）を持った薬品をシステムに認識させることができます。反応ロジックもデータ駆動で拡張可能です。
