# 試験管デバイス開発ログ (test_tube_talk.md)

## 2026-02-13 議事録

### 1. 決定事項・変更点
- **センサーロジックの変更**:
    - 以前: ジャイロセンサー + 加速度センサーの「相補フィルター」を使用。時間経過でドリフト（数値ズレ）が発生し、可動域が狭まる現象があった。
    - 現在: **加速度センサーのみ**を使用するロジックに変更。重力方向を直接計算するため、**時間が経ってもズレない（ドリフトしない）** 仕様になった。
- **座標軸の修正**:
    - 試験管に対する基板の物理配置（USBが上）に対応するため、ファームウェア側で軸を反転 (`ay = -ay`, `ax = -ax`)。
    - 試験管を左右に捻っても判定が逆転しないよう、計算式を `atan2(ax, ay)` (X, Y基準) に統一。
- **可動域の制限 (Frontend)**:
    - 3D表示上での異常回転を防ぐため、**±120度** で回転をストップする制限を入れた。

### 2. 現在の仕様
- **注ぐ角度**: 75度 (プログラム設定値) で液体が出始める。
    - これを **60度** に変更予定。
- **可動域**: 物理的には360度回せるが、画面上は ±120度 にクリップされる。
- **ドリフト問題**: 新しいファームウェア (加速度のみ使用) を書き込むことで解決済み。

### 3. 【重要】持ち方のルール（アナログ的解決）
**「円筒形（試験管）」であるため、持つ向き（捻り）によってセンサーの反応軸が変わってしまう特性があります。**
（例：90度回して持つと、左に倒しても反応しなくなる。180度回すと反応が逆になる）

**解決策: 識別マークを正面にする**
- **ルール:** **「識別LED（または基板のUSBポート）」が自分の方を向くように持つ** こと。
- **理由:** プログラムだけでは「ユーザーが今どっちを正面にして持っているか」を検知できないため（加速度センサーの物理限界）。
- **補助設定 (ToDo):**
    - 試験管の表面に「正面シール」を貼るなどのアナログ的な目印をつけることを推奨。
    - シールや目印が親指に来るように持つことで、常に正しい軸で操作できる。

### 4. 次のアクション
- [済] フロントエンドの「注ぐ角度」のしきい値を 75度 → 60度 に変更する。 (2025-02-13 実施)
- [済] フラスコ内への注水・液面上昇アニメーションの実装。 (2025-02-13 実施)
- [済] 液面が一定量溜まった後に反応を開始するロジックの実装。 (2025-02-13 実施)
- ユーザーにて新しいファームウェアをデバイスに書き込み、動作確認を行う。

### 9. 混合・消失演出の最終調整 (2026-02-24)
- **ユーザー要望の再定義**:
    - 「混ぜるとフラスコの液体は黒くなり、二酸化マンガン（粉末）は消える（見えなくなる）」という挙動へ修正。
    - 以前の「粉末が残る」仕様から変更。

- **実装内容**:
    - 二酸化マンガンのマテリアルを再度 `transparent: true` に戻し、フェードアウト可能な状態へ。
    - 液面が大体3割程度 (`scale.y > 0.3`) まで上昇した段階で、粒子の不透明度 (`opacity`) を徐々に下げて完全に見えなくする処理を追加。
    - これにより、黒い液体（懸濁液）の中に粉末が溶け込んだような見た目を再現。

### 10. 通信APIとデバイス仕様の確認 (2026-02-26)
- **使用API**: Web Bluetooth API (`navigator.bluetooth`)
    - WebHID API (`navigator.hid`) は使用していない。
- **接続手順**:
    1. ブラウザでアプリケーションを開く。
    2. 画面上の「接続」ボタンを押す（セキュリティ制約上、ユーザー操作が必須）。
    3. デバイスリストから試験管デバイスを選択してペアリング。
    - **注意**: デバイスを先に起動していても、ブラウザ側からの呼び出しがないと接続されない。自動再接続機能は現状なし（ページリロードや再接続操作が必要）。
- **Joy-Conとの違い**:
    - 本システムはカスタムGATTサービスの読み取りを行っているため、標準的なHIDデバイス（Joy-Con等）の接続方式とは異なる。

### 11. Joy-Con (node-hid) 接続仕様の確認 (2026-02-26)
- **node-hidの制約**:
    - Bluetooth接続そのもの（ペアリング）を行う機能はない。OS側で接続する必要がある。
    - ホットプラグ（接続検知）イベントがないため、アプリ起動後に接続されたデバイスを検知できない場合がある。
- **対応策**:
    - バックエンド (`JoyConService`) にて、**2秒 (2000ms) 間隔** でデバイスリストをスキャンするポーリング処理を実装。
    - これにより、アプリ起動後にJoy-ConをPCに接続（または再接続）しても、自動的に認識・復帰が可能となる。
    - **手順**: PCでBluetoothオン → アプリ起動 → Joy-Conのボタンを押してPCと接続 → アプリが自動検知。

### 12. 角度デバッグ表示とクエスト判定条件の可視化 (2026-03-03)
- **要望**: クエストのデバッグ用に、試験管とフラスコの角度数値を画面に表示したい。
- **実装内容**:
    - `LabScene.js` にデバッグ表示機能を追加。
    - **表示項目**:
        1. **TestTube (JoyCon L)**: 生データ(Raw)、補正値(Adj)、最終角度(Final)
        2. **Flask (JoyCon R)**: 生データ、補正値、最終角度
        3. **Quest Mixing**: 混ぜ具合の進捗 (Progress / 300.0)
    - これにより、意図した通りの角度で判定されているか、振る強さが足りているかを数値で確認可能になった。
- **クエスト達成条件（再確認）**:
    - 注ぐ: 試験管の傾き < -60度 かつ フラスコの傾き < 10度（安定）
    - 完了: 混ぜ進捗(Mixing Progress) > 300.0 (絶対値積算)


### 13. [修正] フラスコ混合アクションの不具合 (2026-03-03)
- **現象**:
    - フラスコを振っても `Quest Mixing` の値が進まず、反応が完了しない。
- **原因**:
    - 混合判定ロジックが「注ぎ中 (`isPouring`)」の条件ブロック内に記述されていたため、「注ぎ終わった後」にフラスコを振っても反応が進行しなかった。
- **修正内容**:
    - 混合判定ロジックを独立させ、常にフラスコの状態を監視するように変更。
    - **仕様変更**: 酸素発生実験 (`exp_01_o2`) において、フラスコ内に液体がある程度 (`scale.y > 0.2`) 存在する場合、Joy-Con (R) を振ることで常に `Quest Mixing` が進行するようになった。

### 14. トロフィー機能の実装 (2026-03-03)
- **トロフィー獲得ロジック**: 
    - `LabView` にて実験完了時 (`onExperimentComplete`) にトロフィー獲得API (`unlockTrophy`) を呼び出す処理を追加。
    - 酸素発生実験クリア時にトロフィー「酸素マスター (trophy_exp_01_o2)」を獲得可能。
- **トロフィー画面修正**: 
    - UI表示崩れとJSエラーを修正。獲得状況に応じてアイコンのロック/アンロックが切り替わる仕様へ変更。
