# デバイス仕様書 (Test Tube)

## 1. 構成概要
3Dプリンターで印刷した試験管型ケースに、ESP32等の基板を内蔵したデバイス。
傾きを検知してバックエンドサーバーへデータを送信し、Webアプリと連動する。

## 2. 部品表 (BOM)

| 部品名 | 規格・型番 | 個数 | 単価 (概算) | 販売元 | 備考 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **試験管 (筐体)** | 3Dプリント (PLA/Resin) | 1 | 約22円 | 自家製 | 重量: 11.68g |
| **メイン基板** | XIAO ESP32-C3 | 1 | 1,400円 | Amazon | WiFi/BLE内蔵マイコン |
| **バッテリー** | 3.7v 1000mAh 4025100 | 1 | 890円 | Amazon | リチウムポリマー |
| **ジャイロセンサ** | MPU6050 | 1 | 510円 | 秋月電子 | I2C接続 (SDA=Pin6, SCL=Pin7) |
| **識別LED** | ARGB LED (NeoPixel等) | 1 | 14円 | Amazon | 状態表示・識別用 |
| **配線** | 28AWGケーブル | 適量 | 数円 | Amazon | 内部配線用 |

## 3. ソフトウェア仕様 (Firmware)

### 3.1 ネットワーク構成
- **モード:** AP + Station 同時動作 (WIFI_AP_STA)
- **設定用AP (Access Point):**
    - **SSID:** `testtube-[MACアドレス下4桁]`
    - **Password:** `testtube123`
    - **IP:** `192.168.4.1`
    - **機能:** Webサーバー(Port 80)を立ち上げ、接続先Wi-FiとBackendサーバーIPを設定可能。
- **Wi-Fi接続 (Station):**
    - 指定されたSSID/PASSでルーターに接続。
    - 接続成功時のみデータ送信を行う。

### 3.2 データ通信仕様
デバイスからサーバーへの送信仕様。

- **プロトコル:** HTTP POST
- **エンドポイント:** `http://[NODE_IP]:3000/gyro` (※IPは設定画面から変更可能)
- **送信間隔:** 約50ms (20Hz)
- **データ形式 (JSON):**
  ```json
  {
    "id": "testtube-xxxxxx",  // デバイス固有ID (MACアドレスベース)
    "angle": 12.34            // 計算済み傾き角度 (float)
  }
  ```

### 3.3 センサー制御ロジック
- **使用ライブラリ:** `MPU6050.h`
- **取得データ:**
    - 加速度 (Accel): X, Y, Z軸を使用
    - 角速度 (Gyro): 使用しない（ドリフトの原因となるため）
- **角度計算アルゴリズム:** 簡易ローパスフィルタ (加速度ベース)
  ```cpp
  // atan2(x, y) は、座標(x, y)の角度(ラジアン)を返します。
  // 試験管の「左右の傾き」を知りたいので、Y軸(縦)とX軸(横)の関係を見ます。
  // Z軸(奥行き)の変化を計算に含めると、基板の上下関係で逆転しやすいため除外します。

  float accAngle = atan2(ax, ay) * 180 / PI;

  // ※もし左右が逆（左に倒すとプラスになるべきがマイナスになる等）の場合は、
  // 下記のように符号を反転させてください。
  // accAngle = -accAngle;

  // もっと単純に加速度センサーの値だけを使う（ドリフトしない・反応が素直）
  // ビクンビクンするのはFrontend側の平均化処理で消す。
  filterAngle = filterAngle * 0.8 + accAngle * 0.2; // 簡易ローパスフィルタ
  ```
- **特徴:** ジャイロを使用せず、重力加速度の方向のみで角度を決定するため、長時間使用しても値がずれない。試験管を少し捻っても値が反転しにくい。

## 4. クライアント(Frontend)動作仕様
- **傾き検知:** バックエンド経由で受信した角度データを反映。
- **正規化処理:** 受信データが0~360度の場合でも、-180~180度に正規化して処理。
- **表示補正:**
    - **移動平均フィルタ:** 過去5フレームの平均を取りノイズを除去。
    - **不感帯:** 中心付近(±5度)は0度とみなす。
    - **Lerp:** 係数0.08で描画を補間し、滑らかかつ追従性の高い動きを実現。
- **キャリブレーション:** 'C'キー押下で現在の角度を0度とするオフセット設定機能。
### 4.1 フラスコ (Joy-Con R) の挙動仕様 (2025-06-XX 追加)
- **自動キャリブレーション:**
    - アプリ起動時（または実験開始時）、**最初に受信した角度データ** を「0度」として学習する。
    - これにより、Joy-Conの持ち方や初期位置に関わらず、スタート時点を水平とみなして操作が可能。
    - 意図しない「右へ傾き続ける」等のドリフト現象を防止。
- **感度調整:**
    - 試験管と同様の「移動平均5フレーム」「不感帯±5度」「Lerp係数0.1」を採用し、安定性と追従性のバランスを確保。
